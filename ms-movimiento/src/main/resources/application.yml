spring:
  application:
    name: ms-movimiento

  profiles:
    active: dev

  datasource:
    url: ${JDBC_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: oracle.jdbc.OracleDriver

    hikari:
      maximum-pool-size: 10
      data-source-properties:
        oracle.net.tns_admin: C:/Users/Fuyu/Desktop/Duoc/Semestre3/FullStack1
        oracle.net.wallet_location: C:/Users/Fuyu/Desktop/Duoc/Semestre3/FullStack1
        oracle.net.ssl_server_dn_match: false

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.zaxxer.hikari: DEBUG

---

spring:
  config:
    activate:
      on-profile: dev

ms:
  producto:
    url: ${MS_PRODUCTO_URL}
  inventario:
    url: ${MS_INVENTARIO_URL}

---

spring:
  config:
    activate:
      on-profile: remote

ms:
  producto:
    url: lb://ms-producto
  inventario:
    url: lb://ms-inventario